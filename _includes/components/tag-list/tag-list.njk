{% macro tagList(tags, renderer) %}
<details class="tag-list" open>
  <summary>All tags ({{ tags.length }})</summary>
  <ul class="tag-list__list">
    {% for tag in tags %}
      <li class="tag-list__item">
        {{ renderer(tag) }}
      </li>
    {% endfor %}
  </ul>
</details>
{% endmacro %}
